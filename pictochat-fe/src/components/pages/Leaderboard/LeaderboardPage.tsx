import * as React from 'react';
import './LeaderboardPage.less';

interface LeaderboardPage {}

export default (props: LeaderboardPage) => {
  return <section id="leaderboard-page">Leaderboard Page</section>;
};
