import * as React from 'react';
import './DiscussionPage.less';

interface DiscussionPageProps {}

export default (props: DiscussionPageProps) => {
  return <section id="discussion-page">Discussion Page</section>;
};
